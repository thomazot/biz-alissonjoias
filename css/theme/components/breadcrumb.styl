.breadcrumb
    &__home .breadcrumb__link:before
        display: none

    &__link
        color: var(--color-gray)
        font-weight: 500
        font-size: 14px
        line-height: 18px

    &__sep
        color: var(--primary)

        span
            display: none

        &:before
            content: '•'

    &__last
        font-size: 14px